@model IEnumerable<Ejercicio24MVC.Models.Documento>

@{
    ViewBag.Title = "Index";
    Layout = ("~/Views/Shared/_FrontPage.cshtml");
}

<style>
    table {
        margin: 0 auto;
        width: 100%;
        text-align: center;
        font-size: 20px;
    }

    h2 {
        font-size: 40px;
        text-align: center;
    }

    h3 {
        font-size: 20px;
        text-align: center;
    }
</style>

<h2>Documentos</h2>
@if (User.IsInRole("Admin"))
{
    <h3>
        @Html.ActionLink("Crear Documento", "Create", new { title = "Crear un nuevo documento" })
    </h3>
}



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Version)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proyecto.Nombre)
        </th>
        <th></th>
    </tr>


    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Version)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proyecto.Nombre)
            </td>
            @if (User.IsInRole("Admin"))
            {
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.Id })
                    @Html.ActionLink("Detalles", "Details", new { id = item.Id })
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                </td>
            }
            @if (User.IsInRole("Miembros"))
            {
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.Id })
                </td>
            }
        </tr>

    }

</table>
